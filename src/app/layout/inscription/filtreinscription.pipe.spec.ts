import { FiltreinscriptionPipe } from './filtreinscription.pipe';

describe('FiltreinscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreinscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
