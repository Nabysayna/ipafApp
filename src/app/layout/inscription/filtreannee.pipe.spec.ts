import { FiltreanneePipe } from './filtreannee.pipe';

describe('FiltreanneePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreanneePipe();
    expect(pipe).toBeTruthy();
  });
});
