import { FiltresexePipe } from './filtresexe.pipe';

describe('FiltresexePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltresexePipe();
    expect(pipe).toBeTruthy();
  });
});
